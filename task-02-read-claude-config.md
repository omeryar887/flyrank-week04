# MCP Task 2 — Read the Workflow Configuration

## User request to send to the MCP client

> Use the connected `flyrank-week6-files` MCP server to read `week-05-build-no-code-workflow/claude-project-config.md` if that file is inside the allowed project root. If it is not accessible, tell me exactly which path is available. Do not invent the file contents.

## Important note
The package already includes a copy of the Week 5 Claude Project configuration inside `week-05-build-no-code-workflow/` for this demonstration.

## Expected tool
`read_file`

## Evidence to capture
Screenshot showing the MCP tool call and returned file content.
